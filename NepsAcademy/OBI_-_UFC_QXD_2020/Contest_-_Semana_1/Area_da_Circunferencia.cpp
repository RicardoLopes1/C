#include <bits/stdc++.h>

using namespace std;

typedef long long int LL;

int main(){
    double r, pi, a;
    pi = 3.1416;
    cin >> r;
    a = pi*r*r;
    
    cout.precision(2);
    cout.setf(ios::fixed);
    cout << a << endl;

    return 0;
}